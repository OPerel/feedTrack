export enum Times {
  DayInMs = 1000 * 60 * 60 * 24,
  MsPerPx = 1000 * 60 * 3,
}

export const DAYS_TO_LOAD = 7;

export enum Labels {
  AddFeel = 'Add Feel',
  AddMeal = 'Add Meal',
}

export enum Colors {
  Red = '#e72525',
  Orange = '#f17341',
  Yellow = '#daf141',
  Green = '#40e029',
}
